{
  "name": "peterszerzo.com",
  "version": "0.3.0",
  "description": "Source code for my personal website.",
  "main": "index.js",
  "scripts": {
    "copy": "cp -r src/images/* dist/",
    "watchcss": "postcss src/styles.css --use postcss-cssnext --output src/styles.compiled.css --watch",
    "lint": "./node_modules/.bin/stylelint ./src/styles.css && ./node_modules/.bin/standard --fix",
    "deploy": "elm-app build && npm run copy && mv dist/index.html dist/200.html ./node_modules/.bin/surge ./dist peterszerzo.surge.sh"
  },
  "author": "Peter Szerzo <szerzo.peter@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "domready": "^1.0.8",
    "fastclick": "^1.0.6"
  },
  "devDependencies": {
    "standard": "^8.6.0",
    "stylelint": "^7.6.0",
    "surge": "^0.18.0"
  }
}
