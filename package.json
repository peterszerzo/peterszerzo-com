{
  "name": "peterszerzo.com",
  "version": "0.3.0",
  "description": "Source code for my personal website.",
  "main": "index.js",
  "scripts": {
    "start": "elm-app start",
    "build:main": "NODE_ENV=production elm-app build",
    "build:elmcss": "elm-css src/Stylesheets.elm && ./node_modules/.bin/postcss dist/styles.css --output dist/styles.css",
    "build:assets": "cp -r imgs dist",
    "build:html": "mv dist/index.html dist/200.html",
    "build": "rm -rf dist && mkdir dist && npm run build:main && npm run build:elmcss && npm run build:assets && npm run build:html",
    "lint": "./node_modules/.bin/standard src",
    "deploy": "npm run build && echo '\\n\\n ⚠⚠⚠ Nothing deployed yet.. fix html paths in /dist, then run npm run deploy2.'",
    "deploy2": "surge dist peterszerzo.com"
  },
  "author": "Peter Szerzo <szerzo.peter@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.18.0"
  },
  "devDependencies": {
    "autoprefixer": "^6.7.7",
    "postcss-cli": "^3.1.1",
    "standard": "^8.6.0",
    "uglify-js": "^2.8.22"
  }
}
