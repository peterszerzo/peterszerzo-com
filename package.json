{
  "name": "peterszerzo.com",
  "version": "0.3.0",
  "description": "Source code for my personal website.",
  "main": "index.js",
  "scripts": {
    "start:site": "NODE_ENV=development ELM_APP_MAIN=Site elm-app start",
    "start:overeasy": "NODE_ENV=development ELM_APP_MAIN=OverEasy elm-app start",
    "build:main": "NODE_ENV=production elm-app build",
    "build:html": "mv build/index.html build/200.html",
    "build": "rm -rf dist && mkdir dist && npm run build:main && npm run build:html",
    "deploy": "npm run build && surge build peterszerzo.com"
  },
  "author": "Peter Szerzo <szerzo.peter@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "d3-hierarchy": "^1.1.5",
    "elm": "^0.18.0"
  },
  "devDependencies": {
    "create-elm-app": "^1.10.0"
  }
}
