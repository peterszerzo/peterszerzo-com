{
  "name": "peterszerzo.com",
  "version": "0.3.0",
  "description": "Source code for my personal website.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development elm-app start",
    "build:main": "NODE_ENV=production elm-app build",
    "build:elmcss": "elm-css src/Stylesheets.elm && ./node_modules/.bin/postcss build/styles.css --output build/styles.css",
    "build:html": "mv build/index.html build/200.html",
    "build": "rm -rf dist && mkdir dist && npm run build:main && npm run build:elmcss && npm run build:html",
    "lint": "./node_modules/.bin/standard src",
    "deploy": "npm run build && surge build peterszerzo.com"
  },
  "author": "Peter Szerzo <szerzo.peter@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "d3-hierarchy": "^1.1.5",
    "elm": "^0.18.0"
  },
  "devDependencies": {
    "autoprefixer": "^6.7.7",
    "create-elm-app": "^1.10.0",
    "postcss-cli": "^3.1.1"
  }
}
